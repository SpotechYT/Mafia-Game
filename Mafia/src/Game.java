public class Game {
    /*Todo: 
     * Variables: NumPlayer, NumMafia, NumCivilians, NumDetectives, NumDoctors
     * Methods: StartGame(), EndGame(), AssignRoles(), CheckWinCondition()
     * nextRound(), Vote(), ShowRoles()
     */
}
